[project]
name = "database"
version = "0.1.0"
description = "Database session management for pople"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.19.1",
    "psycopg[binary]>=3.3.4",
    "pydantic-settings>=2.14.2",
    "sqlmodel>=0.0.39",
]

[tool.ty.environment]
python = "../../.venv"
